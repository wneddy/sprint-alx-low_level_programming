#include "stdio.h"

/**
 * main - multplying two arguments
 * @argc: counter
 * @argv: storage locater
 *
 * Return: 0 when done
 */

int main(int argc, char **argv)
{
	int d, c, res;

	if (argc == 2)
	{
		res = argv[d] * argv[c];
		printf("%d\n", res);
	}
	else
		return (-1);
}
